# Netlify Build Configuration

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Node.js 버전 지정
  NODE_VERSION = "20"

[context.production]
  # 프로덕션 환경 설정 (필요시)
  environment = {}

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
