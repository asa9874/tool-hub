# 🚀 ToolHub GitHub Pages 배포 가이드

이 가이드는 ToolHub 프로젝트를 GitHub Pages에 배포하는 방법을 단계별로 안내합니다.

---

## 📋 배포 전 체크리스트

배포하기 전에 반드시 아래 항목들을 **실제 값으로 변경**해야 합니다.

### 1️⃣ 필수 수정 항목

| 순서 | 파일 경로 | 수정할 내용 | 예시 |
|------|----------|------------|------|
| 1 | `package.json` | `homepage` URL 변경 | `https://asa9874.github.io/tool-hub` |
| 2 | `vite.config.ts` | `base` 경로 변경 | `/tool-hub/` (레포지토리 이름) |
| 3 | `src/config/siteConfig.ts` | `siteUrl` 변경 | `https://asa9874.github.io/tool-hub` |
| 4 | `scripts/generate-sitemap.mjs` | `SITE_URL` 변경 | `https://asa9874.github.io/tool-hub` |

### 2️⃣ AdSense 설정 (승인 후)

| 파일 | 수정할 내용 |
|------|------------|
| `src/config/siteConfig.ts` | `adsense.clientId`를 실제 ID로 변경<br>`adsense.enabled`를 `true`로 변경 |
| `src/components/AdBanner.tsx` | 각 `slot` 번호를 실제 슬롯 ID로 변경 |

### 3️⃣ 선택 수정 항목

| 파일 | 수정할 내용 | 설명 |
|------|------------|------|
| `index.html` | Google Search Console 인증 코드 | 사이트 소유권 확인용 (등록 후) |
| `src/config/siteConfig.ts` | 사이트 이름, 설명 | SEO용 텍스트 |
| `src/pages/PrivacyPolicy.tsx` | 연락처 이메일 | 실제 연락처로 변경 |

---

## 🔧 1단계: 코드 수정하기

### 1-1. GitHub 레포지토리 확인

먼저 GitHub 레포지토리 정보를 확인합니다:
- **GitHub 사용자명**: 예) `asa9874`
- **레포지토리 이름**: 예) `tool-hub`
- **배포 URL**: `https://asa9874.github.io/tool-hub`

> 💡 **루트 도메인 사용 시**: 레포지토리 이름이 `asa9874.github.io`인 경우, 
> base를 `'/'`로, URL은 `https://asa9874.github.io`로 설정하세요.

---

### 1-2. package.json 수정

**파일**: `package.json`

```json
{
  "homepage": "https://asa9874.github.io/tool-hub",  // ← 여기를 수정
  ...
}
```

**수정 예시**:
```json
{
  "homepage": "https://myname.github.io/my-tools",
  ...
}
```

---

### 1-3. vite.config.ts 수정

**파일**: `vite.config.ts`

```typescript
export default defineConfig({
  plugins: [...],
  base: '/tool-hub/',  // ← 레포지토리 이름으로 변경 (앞뒤 슬래시 필수!)
  ...
});
```

**수정 예시**:
```typescript
base: '/my-tools/',  // 레포지토리 이름이 my-tools인 경우
```

**주의사항**:
- 앞뒤로 `/` 슬래시가 반드시 있어야 합니다!
- 루트 도메인 사용 시: `base: '/'`

---

### 1-4. src/config/siteConfig.ts 수정

**파일**: `src/config/siteConfig.ts`

```typescript
export const siteConfig = {
  // ✅ 실제 배포 URL로 변경
  siteUrl: 'https://asa9874.github.io/tool-hub',
  
  // ✅ 사이트 이름 및 설명 (선택)
  siteName: 'ToolHub - 무료 온라인 도구 모음',
  siteDescription: '만나이 계산기, 글자수 세기...',
  
  // ✅ AdSense 설정 (승인 후 수정)
  adsense: {
    clientId: 'ca-pub-XXXXXXXXXXXXXXXX',  // ← 실제 AdSense ID
    enabled: false,  // ← 승인 후 true로 변경
  },
  
  // ... 나머지 설정
};
```

---

### 1-5. scripts/generate-sitemap.mjs 수정

**파일**: `scripts/generate-sitemap.mjs`

18번째 줄 근처:

```javascript
const SITE_URL = 'https://asa9874.github.io/tool-hub';  // ← 여기를 수정
```

**수정 예시**:
```javascript
const SITE_URL = 'https://myname.github.io/my-tools';
```

---

### 1-6. 새 도구 페이지 추가 시 사이트맵 업데이트

**파일**: `scripts/generate-sitemap.mjs`

새 도구를 추가했다면 사이트맵에 등록하세요:

```javascript
const pages = [
  { path: '/', changefreq: 'daily', priority: '1.0' },
  { path: '/tools/age-calculator', changefreq: 'weekly', priority: '0.9' },
  
  // ✅ 새 도구 추가 예시
  { path: '/tools/character-counter', changefreq: 'weekly', priority: '0.9' },
  { path: '/tools/unit-converter', changefreq: 'weekly', priority: '0.9' },
  
  { path: '/privacy-policy', changefreq: 'monthly', priority: '0.3' },
];
```

그리고 `src/config/siteConfig.ts`의 `tools` 배열에도 추가하세요!

---

## 🚀 2단계: GitHub Pages 배포하기

### 2-1. gh-pages 패키지 설치 확인

이미 설치되어 있어야 합니다. 확인:

```bash
npm list gh-pages
```

설치되지 않았다면:

```bash
npm install gh-pages --save-dev --legacy-peer-deps
```

---

### 2-2. Git 저장소 초기화 (처음 한 번만)

```bash
# Git 초기화
git init

# GitHub 원격 저장소 연결 (레포지토리 URL로 변경)
git remote add origin https://github.com/asa9874/tool-hub.git

# 첫 커밋
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

---

### 2-3. 배포 실행

```bash
npm run deploy
```

이 명령어는 자동으로:
1. 사이트맵 생성
2. 프로젝트 빌드
3. `gh-pages` 브랜치에 배포

**출력 예시**:
```
📄 Step 1: 사이트맵 및 robots.txt 생성
✅ sitemap.xml 생성 완료
✅ robots.txt 생성 완료

🔨 Step 2: 프로젝트 빌드
✓ built in 3.45s

📤 Step 3: gh-pages 브랜치에 배포
Published

✅ 배포 완료!
```

---

### 2-4. GitHub Pages 설정

1. GitHub 레포지토리로 이동
2. `Settings` → `Pages` 메뉴 클릭
3. **Source** 섹션에서:
   - Branch: `gh-pages` 선택
   - Folder: `/ (root)` 선택
4. **Save** 클릭

5-10분 후 `https://asa9874.github.io/tool-hub`에 접속하여 확인!

---

## ✅ 3단계: 배포 후 확인사항

### 체크리스트

- [ ] 사이트가 정상적으로 로드되는가?
- [ ] 홈페이지에서 모든 도구 링크가 작동하는가?
- [ ] 좌측 사이드바 메뉴가 정상 작동하는가?
- [ ] 각 도구 페이지가 제대로 표시되는가?
- [ ] 브라우저 새로고침 시 404 에러가 발생하지 않는가?
- [ ] 모바일에서도 정상 작동하는가?
- [ ] `/sitemap.xml` 접속 시 사이트맵이 보이는가?
- [ ] `/robots.txt` 접속 시 robots.txt가 보이는가?

---

## 🔍 4단계: Google Search Console 등록

### 4-1. Search Console에 사이트 추가

1. [Google Search Console](https://search.google.com/search-console) 접속
2. **속성 추가** 클릭
3. **URL 접두어** 선택 후 사이트 URL 입력
4. 소유권 확인 방법 중 **HTML 태그** 선택
5. 제공된 메타 태그를 `index.html`에 추가:

**파일**: `index.html`

```html
<head>
  ...
  <!-- Google 사이트 소유권 확인 -->
  <meta name="google-site-verification" content="YOUR_VERIFICATION_CODE" />
  ...
</head>
```

6. 다시 배포: `npm run deploy`
7. Search Console에서 **확인** 클릭

---

### 4-2. 사이트맵 제출

1. Search Console → **Sitemaps** 메뉴
2. `sitemap.xml` 입력 후 **제출**
3. 상태가 **성공**으로 표시되는지 확인

---

## 💰 5단계: Google AdSense 신청 (선택)

### 승인 받기 전

1. 최소 15-20개 이상의 유용한 도구 페이지 제작
2. 각 페이지마다 충분한 설명 텍스트 (최소 300자 이상)
3. 개인정보처리방침 페이지 필수
4. 1-2개월 동안 꾸준히 콘텐츠 추가

### AdSense 승인 후

1. AdSense에서 **광고 단위** 생성
2. `src/config/siteConfig.ts` 수정:

```typescript
adsense: {
  clientId: 'ca-pub-1234567890123456',  // ← 실제 ID로 변경
  enabled: true,  // ← true로 변경
},
```

3. `src/components/Layout.tsx`에서 광고 슬롯 번호 변경:

```tsx
<AdBanner slot="1234567890" format="horizontal" />
<AdBanner slot="0987654321" format="horizontal" />
```

4. 다시 배포: `npm run deploy`

---

## 🛠️ 문제 해결

### 문제 1: 배포 후 404 에러 발생

**원인**: `base` 경로가 잘못 설정됨

**해결**:
1. `vite.config.ts`에서 `base` 확인
2. 레포지토리 이름과 일치하는지 확인
3. 앞뒤 슬래시(`/`) 확인

---

### 문제 2: CSS/JS 파일이 로드되지 않음

**원인**: `base` 경로 불일치

**해결**:
1. `package.json`의 `homepage` 확인
2. `vite.config.ts`의 `base` 확인
3. 두 값이 일치하는지 확인

---

### 문제 3: 새로고침 시 404 에러

**원인**: `public/404.html`이 없거나 잘못됨

**해결**:
1. `public/404.html` 파일이 존재하는지 확인
2. 파일 내용에 `pathSegmentsToKeep` 값 확인
   - 서브 경로 사용: `1`
   - 루트 도메인: `0`

---

### 문제 4: gh-pages 브랜치가 생성되지 않음

**원인**: Git 저장소가 초기화되지 않음

**해결**:
```bash
git init
git remote add origin https://github.com/asa9874/tool-hub.git
git add .
git commit -m "Initial commit"
git push -u origin main
npm run deploy
```

---

### 문제 5: 사이트맵이 업데이트되지 않음

**해결**:
```bash
# 수동으로 사이트맵 생성
npm run generate-sitemap

# 다시 배포
npm run deploy
```

---

## 📝 일상적인 배포 워크플로우

### 새 기능 추가 후 배포

```bash
# 1. 코드 수정
# 2. Git 커밋
git add .
git commit -m "새 도구 추가: 글자수 세기"

# 3. GitHub에 푸시
git push origin main

# 4. 배포
npm run deploy
```

### 새 도구 추가 체크리스트

- [ ] `src/pages/tools/` 폴더에 도구 컴포넌트 생성
- [ ] `src/App.tsx`에 라우트 추가
- [ ] `src/config/siteConfig.ts`의 `tools` 배열에 추가
- [ ] `scripts/generate-sitemap.mjs`의 `pages` 배열에 추가
- [ ] SEO 메타 태그 설정 (title, description, keywords)
- [ ] 테스트 후 배포

---

## 🎯 최적화 팁

### SEO 최적화

1. **각 페이지마다 고유한 메타 태그** 설정
2. **H1 태그는 페이지당 1개만** 사용
3. **구조화된 데이터**(Schema.org) 추가
4. **FAQ 섹션** 추가 (검색 노출에 유리)
5. **내부 링크** 적극 활용

### 성능 최적화

1. 이미지는 WebP 포맷 사용
2. 이미지에 `loading="lazy"` 속성 추가
3. 불필요한 라이브러리 제거
4. 코드 스플리팅 활용 (이미 설정됨)

---

## 📞 도움이 필요하신가요?

- **GitHub Issues**: 프로젝트 레포지토리에서 이슈 생성
- **공식 문서**:
  - [GitHub Pages 공식 문서](https://docs.github.com/ko/pages)
  - [Vite 공식 문서](https://vitejs.dev/)
  - [React Router 공식 문서](https://reactrouter.com/)

---

## ✨ 배포 완료!

축하합니다! 이제 전 세계 사람들이 당신의 도구를 사용할 수 있습니다! 🎉

정기적으로 새로운 도구를 추가하고, SEO를 최적화하여 더 많은 사용자를 유치하세요.
